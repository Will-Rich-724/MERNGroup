import React, { useState, useEffect } from 'react';
import { Router, Link, navigate } from '@reach/router';
import axios from 'axios';

const Admin = (props) => {

    //axios get all appointments for a given location

    return(
        <div>
            <form>

            </form>
        </div>
    )
}

export default Admin;